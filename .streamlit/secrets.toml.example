# Copie este arquivo para secrets.toml e preencha a chave.
# secrets.toml está no .gitignore e nunca deve ser versionado.
#
# No Streamlit Community Cloud, o mesmo conteúdo vai em
# Manage app → Settings → Secrets, sem criar arquivo no repositório.

COINGECKO_API_KEY = "CG-sua-chave-aqui"
